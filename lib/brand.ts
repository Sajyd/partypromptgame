export const APP_NAME = "Promptjam";

export const APP_TAGLINE = "Party games you build together";

/** Full `<title>` for metadata */
export const APP_TITLE = `${APP_NAME} — ${APP_TAGLINE}`;

export const APP_DESCRIPTION =
  "A multiplayer party app where friends prompt different parts of a tiny game, stitch it together with AI, and play it in minutes.";

/** zustand `persist` storage name — changing this clears prior local data */
export const PERSIST_STORAGE_KEY = "promptjam-store";
